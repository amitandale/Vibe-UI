const { test } = require('@playwright/test');

// Placeholder test to keep workflow green until real E2E specs are added.
test.skip('placeholder e2e', async ({ page }) => {
  // Intentionally empty.
});
